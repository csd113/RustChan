├── Cargo.lock
├── Cargo.toml
├── CHANGELOG.md
├── deny.toml
├── dev-check-strict.sh
├── docs
│   ├── build_all_rustchan.sh
│   ├── channet_build_plan.md                # ChanNet + RustWave gateway implementation plan. Authoritative source of truth
│   │                                        # for all /chan/* API work. Includes RustWave API reference for external devs.
│   ├── channet_impl_plan.md
│   └── phase6a6b.txt
├── LICENSE
├── README.md
├── SETUP.md
├── src
│   ├── chan_net                              # ChanNet API module. Runs on a second TCP listener (default 127.0.0.1:7070),
│   │   │                                    # separate from the main forum port. Activated with the --chan-net CLI flag.
│   │   │                                    # Serves two independent layers: federation sync (node-to-node ZIP exchange)
│   │   │                                    # and the RustWave gateway (JSON command in, ZIP data package out).
│   │   ├── command.rs                       # RustWave gateway handler. Fully implemented in Phase 7 (Step 7.2).
│   │   │                                    # POST /chan/command accepts a raw JSON body and dispatches via the Command
│   │   │                                    # enum (#[serde(tag = "type", rename_all = "snake_case")]). Commands:
│   │   │                                    # full_export, board_export, thread_export, archive_export, force_refresh,
│   │   │                                    # reply_push. Returns application/zip with Content-Disposition filename
│   │   │                                    # following the build-plan naming convention (e.g. rustchan_full_<ts>.zip,
│   │   │                                    # rustchan_thread_<id>_reply_confirmed_<ts>.zip). reply_push validates
│   │   │                                    # content <= 32,768 chars and author <= 255 chars before any DB write.
│   │   │                                    # DB/builder errors map to 400; tokio join errors map to 500 via
│   │   │                                    # anyhow::anyhow!(e). db.get() uses ? directly (From<r2d2::Error>).
│   │   │                                    # Text content only — no media fields ever cross this interface.
│   │   ├── export.rs                        # Federation export handler. POST /chan/export acquires a DB connection, runs
│   │   │                                    # snapshot::build_snapshot on a blocking thread, and returns ZIP bytes with
│   │   │                                    # Content-Type: application/zip. Fully implemented in Phase 2 (Step 2.4).
│   │   ├── import.rs                        # Federation import handler. POST /chan/import receives a snapshot ZIP, checks
│   │   │                                    # TxLedger for duplicates (AppError::Conflict on replay), validates schema
│   │   │                                    # (non-empty board, ≤32 768-char content), writes boards and posts to the
│   │   │                                    # chan_net_posts mirror table in a single spawn_blocking, then records the
│   │   │                                    # tx_id in the ledger. do_import() is shared with poll.rs. Ed25519 signature
│   │   │                                    # branch: logs a warning and continues if metadata.signature.is_some() —
│   │   │                                    # verification is stubbed pending Phase N implementation. Fully implemented
│   │   │                                    # in Phase 3 (Step 3.3); hardened in Phase 6 (Step 6.3).
│   │   ├── ledger.rs                        # TxLedger: in-memory HashSet<Uuid> deduplication for federation imports.
│   │   │                                    # Cleared on server restart; DB unique index provides persistent safety net.
│   │   │                                    # NOTE: TxLedger itself is not stored in AppState — AppState holds the inner
│   │   │                                    # Arc<Mutex<HashSet<Uuid>>> directly (minimal approach from Step 1.5).
│   │   │                                    # TxLedger remains available for any future code that prefers the typed API.
│   │   ├── mod.rs                           # ChanNet module root. chan_router() wires all /chan/* routes onto the second
│   │   │                                    # listener. Applies DefaultBodyLimit per route: chan_net_max_body for
│   │   │                                    # /chan/import (ZIP snapshots, default 10 MiB), chan_net_command_max_body
│   │   │                                    # for /chan/command (JSON commands, default 8 KiB). Rate-limit middleware is
│   │   │                                    # intentionally excluded — all traffic is machine-to-machine.
│   │   ├── poll.rs                          # Federation incoming handler. POST /chan/poll drains RustWave's broadcast
│   │   │                                    # queue by fetching and importing up to MAX_POLL_ITERATIONS (50) snapshots
│   │   │                                    # per call. GET {rustwave_url}/broadcast/incoming per iteration. Reads the
│   │   │                                    # full response body into bytes once (single-pass stream), then checks for
│   │   │                                    # the {"status":"empty"} JSON sentinel before attempting ZIP import.
│   │   │                                    # Skips AppError::Conflict silently; propagates all other errors.
│   │   │                                    # Reuses HTTP_CLIENT from refresh.rs and do_import() from import.rs.
│   │   │                                    # Returns {"imported": N} on success. Fully implemented in Phase 5 (Step 5.1).
│   │   ├── refresh.rs                       # Federation outgoing handler. POST /chan/refresh builds a full snapshot and
│   │   │                                    # pushes it to RustWave /broadcast/transmit as multipart. Holds the shared
│   │   │                                    # HTTP_CLIENT static (LazyLock<reqwest::Client>) reused by poll.rs.
│   │   │                                    # Returns { status, local_tx_id, broadcast_tx_id } JSON on success.
│   │   │                                    # db.get() uses ? directly (From<r2d2::Error>); JoinError and reqwest
│   │   │                                    # errors are wrapped via anyhow::anyhow!(e). Fully implemented in Phase 4.
│   │   ├── selective_snapshot.rs            # RustWave gateway snapshot builders. Fully implemented in Phase 7 (Step 7.1).
│   │   │                                    # Five builders produce scoped ZIP packages (boards.json, threads.json,
│   │   │                                    # posts.json, metadata.json): build_full_snapshot (all boards, active only,
│   │   │                                    # optional since delta), build_board_snapshot (one board, optional since delta),
│   │   │                                    # build_thread_snapshot (one thread, optional since delta),
│   │   │                                    # build_archive_snapshot (archived threads for one board, no since support),
│   │   │                                    # build_force_refresh_snapshot (everything including archives, no timestamp
│   │   │                                    # filtering, emits tracing::warn!). GwMetadata carries scope, is_delta,
│   │   │                                    # includes_archive, since fields. GwPost/GwThread/GwBoard carry text fields
│   │   │                                    # only — no media columns. Column names verified against src/db/posts.rs
│   │   │                                    # (p.name, p.body) and src/db/threads.rs (t.subject, t.archived).
│   │   │                                    # Board queries use SELECT short_name, name (not title) — verified against
│   │   │                                    # boards table schema. fetch_posts() uses dynamic SQL with four match arms.
│   │   ├── snapshot.rs                      # Federation snapshot structs and builders. SnapshotBoard, SnapshotPost,
│   │   │                                    # SnapshotMetadata (with since, is_delta, includes_archive fields).
│   │   │                                    # build_snapshot() queries all boards and active posts (WHERE archived = 0,
│   │   │                                    # no media columns, SELECT short_name, name — verified against boards schema)
│   │   │                                    # and produces an in-memory ZIP. unpack_snapshot() validates ZIP contents
│   │   │                                    # against a strict three-file whitelist before deserialising.
│   │   │                                    # Fully implemented in Phase 2 (Steps 2.1–2.3).
│   │   └── status.rs                        # GET /chan/status — health check returning service name, version, board count,
│   │                                        # and post count as JSON. Used by operators and RustWave to verify connectivity.
│   │                                        # Queries COUNT(*) from boards and posts via spawn_blocking. Fully implemented
│   │                                        # in Phase 6 (Step 6.1).
│   ├── config.rs                            # Runtime configuration loader. Priority: env vars → settings.toml → hard-coded defaults.
│   │                                        # Auto-generates settings.toml on first run. Manages cookie_secret (never left at default).
│   │                                        # ChanNet fields: rustwave_url (validated http/https at startup via Config::validate()),
│   │                                        # chan_net_bind, chan_net_max_body (ZIP imports, default 10 MiB),
│   │                                        # chan_net_command_max_body (JSON commands, default 8 KiB).
│   ├── db
│   │   ├── admin.rs                         # Admin-facing DB queries: admin users & sessions, bans, word filters, user reports,
│   │   │                                    # mod log, ban appeals, IP history, WAL checkpoint, VACUUM, DB size.
│   │   ├── boards.rs                        # Board-level queries: site_settings CRUD, board CRUD, delete_board (with file-safety
│   │   │                                    # guard), aggregate site statistics. Optimised with single-pass aggregate queries.
│   │   ├── chan_net.rs                       # ChanNet DB helpers. Three functions: insert_board_if_absent (idempotent SELECT-
│   │   │                                    # before-INSERT; writes to boards.name column — verified against schema),
│   │   │                                    # insert_post_if_absent (INSERT OR IGNORE into chan_net_posts mirror table),
│   │   │                                    # insert_reply_into_thread (INSERT into live posts table — the only write path
│   │   │                                    # from the RustWave gateway; validates thread existence, board membership, and
│   │   │                                    # archive status). Writes to name/body/body_html columns (verified against
│   │   │                                    # posts.rs schema); ip_hash = NULL; deletion_token = UUID v4. Bumps reply_count
│   │   │                                    # and bumped_at on success (non-co-transactional, MED-6 documented limitation).
│   │   ├── mod.rs                           # Database layer entry point: r2d2 connection pool, schema init & migrations,
│   │   │                                    # paths_safe_to_delete helper, WAL+NORMAL-sync config. Re-exports all sub-modules.
│   │   │                                    # Migrations v25–v26 add the chan_net_posts mirror table and its unique index on
│   │   │                                    # (remote_post_id, board_id) for deduplication. Fully implemented in Phase 3.
│   │   ├── posts.rs                         # Post CRUD, file deduplication, polls, and the background job queue.
│   │   │                                    # Includes worker-side update helpers and LIKE-escape utilities.
│   │   └── threads.rs                       # Thread-level queries: listing, creation (atomic with OP post via transaction),
│   │                                        # sticky/lock toggles, deletion, archive/prune logic.
│   ├── detect.rs                            # Startup detection for optional external tools (ffmpeg, Tor).
│   │                                        # Launches an isolated Tor hidden-service process that coexists with system Tor.
│   ├── error.rs                             # Unified AppError type. Each variant maps 1:1 to an HTTP status code (400/403/404/
│   │                                        # 413/415/409/429/503/500) and auto-converts to an HTML error response via
│   │                                        # IntoResponse. Conflict(String) variant added for 409 responses used by the
│   │                                        # federation import layer (do_import) and handled in poll.rs match arm.
│   │                                        # Internal(anyhow::Error) — all chan_net callers use anyhow::anyhow!() or direct
│   │                                        # ? propagation; passing String to this variant is a compile error.
│   ├── handlers
│   │   ├── admin
│   │   │   ├── auth.rs                      # Admin login/logout/session management. Argon2 password verify, session cookie
│   │   │   │                                # (HTTPOnly, SameSite=Strict). Brute-force lockout: 5 failures / 15 min per IP.
│   │   │   ├── backup.rs                    # Backup and restore subsystem: full-site backups, per-board backups, streaming
│   │   │   │                                # downloads, saved-backup restore, live board.json restore.
│   │   │   ├── content.rs                   # Board/thread/post management handlers (create board, delete thread, delete post).
│   │   │   │                                # All routes require a valid admin session cookie.
│   │   │   ├── mod.rs                       # Admin panel module root. Shared SESSION_COOKIE constant, CSRF helpers, and
│   │   │   │                                # require_admin_session* guards used across all admin sub-modules.
│   │   │   ├── moderation.rs                # Moderation handlers: add/remove bans, ban appeals, word filters, reports,
│   │   │   │                                # IP history, mod log. All routes require a valid admin session cookie.
│   │   │   └── settings.rs                  # Board settings, site settings, and maintenance (VACUUM) handlers.
│   │   │                                    # All routes require a valid admin session cookie.
│   │   ├── board.rs                         # Public board handlers: GET / (board list), GET /:board/ (thread index),
│   │   │                                    # POST /:board/ (create thread), catalog, search, user post deletion.
│   │   ├── mod.rs                           # Handlers module root. Shared streaming multipart parser (read_field_bytes)
│   │   │                                    # that enforces per-type size limits without buffering entire files in memory.
│   │   └── thread.rs                        # Thread view/reply handlers: GET /:board/thread/:id, POST reply, POST poll vote.
│   ├── lib.rs                               # Library crate root. Re-exports config, db, error, media, models, templates, utils
│   │                                        # for use by both the server binary and admin CLI tooling.
│   ├── logging.rs                           # Structured logging init (tracing). Two layers: stderr (human-readable, info+)
│   │                                        # and file (JSON, debug+, written to rustchan.log). Respects RUST_LOG env var.
│   ├── main.rs                              # Binary entry point. Dispatches CLI subcommands (serve, admin create-admin,
│   │                                        # reset-password, list-admins, board CRUD, ban management). Builds Tokio runtime
│   │                                        # manually to expose blocking_threads tuning.
│   │                                        # --chan-net flag activates the second TCP listener on chan_net_bind.
│   │                                        # Conditionally constructs chan_ledger (Arc<Mutex<HashSet<Uuid>>>) in AppState.
│   ├── media
│   │   ├── convert.rs                       # Per-format conversion logic. jpg/gif/bmp/tiff → WebP; png → WebP only if smaller;
│   │   │                                    # svg/webp/webm/audio/mp4 kept as-is. All conversions require ffmpeg.
│   │   ├── exif.rs                          # EXIF orientation helpers for decoded images. Reads Orientation tag from JPEG bytes
│   │   │                                    # and applies the matching pixel transform so thumbnails render upright.
│   │   ├── ffmpeg.rs                        # FFmpeg binary detection and subprocess execution helpers. Uses explicit arg arrays
│   │   │                                    # (no shell strings). Returns trimmed stderr on non-zero exit for operator diagnostics.
│   │   ├── mod.rs                           # Public media pipeline interface. MediaProcessor::process_upload() orchestrates
│   │   │                                    # conversion, thumbnail generation, and graceful ffmpeg degradation. Exposes
│   │   │                                    # ProcessedMedia result struct with final path, mime, size, and conversion flag.
│   │   └── thumbnail.rs                     # Thumbnail generation for all media types. Output: WebP, max 250×250, quality 80.
│   │                                        # Video/WebM → ffmpeg first-frame. Images → image crate fallback if no ffmpeg.
│   │                                        # Static SVG placeholders for video and audio when ffmpeg is unavailable.
│   ├── middleware
│   │   └── mod.rs                           # Two middleware systems: (1) in-memory sliding-window rate limiter for GET requests
│   │                                        # (anti-scraping; excludes /static, /boards, /admin, /api, admin sessions);
│   │                                        # (2) double-submit CSRF protection; (3) IP extraction that trusts X-Real-IP
│   │                                        # (not X-Forwarded-For) when behind a proxy to prevent IP-spoofing.
│   │                                        # AppState.chan_ledger: Option<Arc<parking_lot::Mutex<HashSet<uuid::Uuid>>>>,
│   │                                        # populated only when --chan-net flag is active. The raw HashSet approach
│   │                                        # (not TxLedger struct) avoids a layering inversion from the lib crate.
│   ├── models.rs                            # Plain data structs mapping 1:1 to DB rows (no ORM). Includes MediaType enum
│   │                                        # (Image/Video/Audio) with MIME and extension inference, Board, Thread, Post,
│   │                                        # Ban, WordFilter, Poll, BackupInfo, pagination and search types.
│   ├── server
│   │   ├── cli.rs                           # Clap CLI type definitions (Cli, Command, AdminAction) and synchronous run_admin()
│   │   │                                    # that executes admin subcommands directly against the DB without starting HTTP.
│   │   │                                    # Serve variant includes --chan-net flag (chan_net: bool) that activates the
│   │   │                                    # second TCP listener when passed to run_server().
│   │   ├── console.rs                       # Terminal stats display and interactive keyboard console. Computes req/s, uptime,
│   │   │                                    # board/thread/post counts, DB size, and active uploads from global atomics + DB.
│   │   ├── mod.rs                           # Server subsystem module root. Re-exports run_server and global request-counter
│   │   │                                    # atomics (REQUEST_COUNT, IN_FLIGHT, ACTIVE_UPLOADS, SPINNER_TICK, ACTIVE_IPS).
│   │   └── server.rs                        # HTTP server runtime: Axum router wiring, background task spawning (session purge,
│   │                                        # WAL checkpoint, IP prune, VACUUM, poll cleanup, thumb-cache eviction), embedded
│   │                                        # static asset handlers, HSTS middleware, graceful shutdown (Ctrl-C / SIGTERM).
│   │                                        # run_server() accepts chan_net: bool; when true, populates chan_ledger in AppState
│   │                                        # and spawns a second TcpListener on chan_net_bind serving chan_router().
│   ├── templates
│   │   ├── admin.rs                         # Admin page templates: login form, main control panel, mod log, VACUUM result,
│   │   │                                    # IP history page. Pure Rust string generation — no template engine.
│   │   ├── board.rs                         # Board page templates: site home (board list), board thread index with pagination,
│   │   │                                    # grid catalog view, archived threads list, search results.
│   │   ├── forms.rs                         # HTML form fragments (not full pages): new-thread form and reply form snippets
│   │   │                                    # embedded into board and thread templates. Handles PoW captcha and audio combos.
│   │   ├── mod.rs                           # Templates module root. Shared helpers: base_layout, fmt_ts, render_pagination,
│   │   │                                    # escape_html wrapper. Live-updating in-memory caches for site name, subtitle,
│   │   │                                    # default theme, and board list (parking_lot RwLock + Arc<str>).
│   │   └── thread.rs                        # Thread page templates: full thread with posts and reply form, single post HTML
│   │                                        # (reused by board.rs for index previews), and embedded poll widget.
│   ├── utils
│   │   ├── crypto.rs                        # Security primitives: Argon2id password hashing (t=2, m=64MiB, p=2), SHA-256 IP
│   │   │                                    # hashing (salted with cookie_secret), CSRF tokens, session IDs, deletion tokens,
│   │   │                                    # proof-of-work captcha verification. All tokens use OsRng (OS CSPRNG).
│   │   ├── files.rs                         # Upload pipeline: MIME validation (magic bytes + extension), size enforcement,
│   │   │                                    # UUID filename generation, EXIF stripping, thumbnail creation. Returns UploadedFile
│   │   │                                    # with processing_pending flag for async ffmpeg jobs. Never trusts Content-Type alone.
│   │   ├── mod.rs                           # Utils module root. Re-exports crypto, files, sanitize, tripcode sub-modules.
│   │   ├── sanitize.rs                      # XSS prevention and post markup pipeline: escape_html, greentext, >>reply links,
│   │   │                                    # cross-board links, URL hyperlinking, **bold**, __italic__, [spoiler], dice rolls,
│   │   │                                    # video embed extraction, word filter application.
│   │   └── tripcode.rs                      # Tripcode system: splits "Name#password" → "Name!XXXXXXXXXX" using SHA-256
│   │                                        # (truncated to 10 base64url chars). Stable output; same password = same tripcode.
│   └── workers
│       └── mod.rs                           # SQLite-backed persistent background job queue and Tokio worker pool.
│                                            # Jobs: VideoTranscode (MP4→WebM), AudioWaveform (waveform PNG), ThreadPrune,
│                                            # SpamCheck. Atomic job claiming via UPDATE…RETURNING, retry budget (3 attempts),
│                                            # CancellationToken for graceful shutdown drain.
├── static
│   ├── main.js                              # Client-side JS: post interactions, PoW captcha solver, media lightbox, auto-update.
│   ├── style.css                            # Site stylesheet (compiled into binary via include_str! in server.rs).
│   └── theme-init.js                        # Early theme initialiser (prevents flash-of-wrong-theme on page load).
└── ToDO.txt
